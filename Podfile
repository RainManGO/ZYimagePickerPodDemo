# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ZYimagePickerDisplayDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ZYimagePickerDisplayDemo
   pod 'ZYImagePickerAndBrower', '~> 1.0.0'
   pod 'ZYImagePickerLayoutView', '~> 0.1.7'

  target 'ZYimagePickerDisplayDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ZYimagePickerDisplayDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
